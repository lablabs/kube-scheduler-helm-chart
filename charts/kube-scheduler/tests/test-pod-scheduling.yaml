apiVersion: v1
kind: Pod
metadata:
  name: scheduling-test
  annotations:
    "helm.sh/hook": test
spec:
  schedulerName: project-scheduler
  containers:
    - name: scheduling-test
      image: alpine:latest
      command: ["/bin/sh", "-c", 'echo "Test Pod was scheduled successfully"']
  restartPolicy: Never
